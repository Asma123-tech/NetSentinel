# requirements.txt — UPDATED (AI image moderation enabled)

# ── Original dependencies (unchanged) ────────────────────────
fastapi
uvicorn[standard]

SQLAlchemy
psycopg2-binary

pydantic
pydantic-settings

requests

# For CORS middleware
python-multipart

# ── Image moderation ──────────────────────────────────────────
# Pillow for blur rendering
Pillow

# NudeNet — detects exposed body parts using ONNX
nudenet

# PyTorch CPU-only + HuggingFace — NSFW classifier fallback
# Using extra-index-url for the lightweight CPU build (~200MB vs ~2GB GPU)
torch --index-url https://download.pytorch.org/whl/cpu
transformers

# ── MODULE 4: Authentication & Session Management ─────────────
python-jose[cryptography]   # JWT creation and validation
passlib[bcrypt]             # bcrypt password hashing
bcrypt==4.0.1
email-validator             # email field validation in Pydantic